create table EMPLOYEES(
                          id bigint not null,
                          name varchar(255),
                          primary key (id)
);

-- file was refactored to shema2 so that spring doesn't recognize it.